# @agentcom/client

AgentCom WebSocket client, credentials, config, and device authentication helpers.

Most Pi users should install `pi-agentcom` instead; this package is a runtime dependency used by the Pi extension and other AgentCom clients.
